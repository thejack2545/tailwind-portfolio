/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // 👈 เพื่อให้ toggle ได้
    content: ["./*.html"],
    theme: {
      extend: {},
    },
    plugins: [],
  }
  